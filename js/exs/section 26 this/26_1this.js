// ex1
// it will print the body if it's in the body,if not it will
// print the window

// ex2
// a
// undefinded or if the window have a property of name-it will print it,
// because its a arrow func which has no scope
// b
// change the arrow func to func exp

// ex3
// nothing the func doenst invoke

// ex4
// where ever it will invoke it will print the father

// ex5
// a
// nothig because there isnt a eventype,but if there is one
// it will print the window because arrow func doesn't have
// a scope
// b
// change the arrow func to func exp
